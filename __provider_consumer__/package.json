{
  "name": "@datalayer-examples/provider-consumer-extensions",
  "private": true,
  "scripts": {
    "install": "lerna run install",
    "dev": "lerna run dev",
    "build": "lerna run build",
    "watch": "lerna run watch",
    "clean": "lerna run clean"
  },
  "workspaces": [
    "./packages/*"
  ],
  "devDependencies": {
    "concurrently": "^6.2.0",
    "lerna": "^6.6.2",
    "npm-run-all": "^4.1.5"
  }
}
